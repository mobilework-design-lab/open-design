# 布局与层级

- 每页只有一个首要阅读路径或操作目标。DOM 顺序、视觉顺序和键盘顺序保持一致。
- 建立可复用的间距节奏；相关项更近、分组之间更远。边框、阴影和卡片不是默认分组方式。
- 内容宽度由任务决定：阅读内容收窄，表格和操作台可更宽更密；密度必须匹配受众频率和数据量。
- 用对齐、比例、留白和少量表面层级建立结构。避免所有区块都成为相同圆角卡片。
- 非对称构图必须服务于重点、流向或品牌气质；不能以破坏扫描效率为代价。
- 首屏应让用户在数秒内识别产品、当前上下文和主要下一步。长页面保持标题、导航或区块锚点的一致定位。
